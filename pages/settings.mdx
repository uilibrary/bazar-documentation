<h1 className="text-primary">Settings</h1>

- Change layout direction settings from `src/contexts/SettingsContext.tsx`
- Access settings and update settings via useSettings `src/hooks/useSettings`

```ts
const initialSettings = {
  direction: 'ltr',
}
```
