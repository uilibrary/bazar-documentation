# Shop Layout 3

import { Callout } from 'nextra-theme-docs'

<Callout>Folder location: `src/components/layouts/shop-layout-3`</Callout>

<br />

<img src="/layout-3.jpg" width="400" />

## Examples

```jsx {1,6} copy showLineNumbers
import ShopLayout3 from 'components/layouts/shop-layout-3'
import api from 'utils/__api__/layout'

export default async function Example1({ children }) {
  const data = await api.getLayoutData()
  return <ShopLayout3 data={data}>{children}</ShopLayout3>
}
```

```jsx {1,8} copy showLineNumbers
import ShopLayout3 from 'components/layouts/shop-layout-3'
import api from 'utils/__api__/layout'

export default async function Example2({ children }) {
  const data = await api.getLayoutData()

  return (
    <ShopLayout3 centeredNavigation showMobileMenu={false} data={data}>
      {children}
    </ShopLayout3>
  )
}
```

```jsx {1,8-13} copy showLineNumbers
import ShopLayout3 from 'components/layouts/shop-layout-3'
import api from 'utils/__api__/layout'

export default async function Example3({ children }) {
  const data = await api.getLayoutData()

  return (
    <ShopLayout3
      centeredNavigation
      showMobileMenu={false}
      showFooter={false}
      data={data}
    >
      {children}
    </ShopLayout3>
  )
}
```

```jsx {1, 8} copy showLineNumbers
import ShopLayout3 from 'components/layouts/shop-layout-3'
import api from 'utils/__api__/layout'

export default async function Example4({ children }) {
  const data = await api.getLayoutData()

  return (
    <ShopLayout3 centeredNavigation data={data}>
      {children}
    </ShopLayout3>
  )
}
```

## Inside Components

- [Topbar](https://bazaar-doc.netlify.app/layout-components/topbar)
- [Header](https://bazaar-doc.netlify.app/layout-components/header)
- [Footer](https://bazaar-doc.netlify.app/layout-components/footer)

## Used

- [Medical](https://bazar-react.vercel.app/medical)
- [Grocery-1](https://bazar-react.vercel.app/grocery-1)
- [Fashion-3](https://bazar-react.vercel.app/fashion-3)

## Props

| Name               |   Type    | Required | Default |
| :----------------- | :-------: | -------: | ------- |
| data               |  Object   |      Yes | -       |
| children           | ReactNode |      Yes | -       |
| showFooter         |  boolean  | Optional | true    |
| showMobileMenu     |  boolean  | Optional | true    |
| centeredNavigation |  boolean  | Optional | false   |
