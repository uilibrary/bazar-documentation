<h1 className="text-primary">Dashboard Navbar</h1>

It's only used for vendor/admin dashboard.

Folder location: `src/components/layouts/vendor-dashboard/DashboardNavbar`

##### Example - `DashboardNavbar` example code

```jsx
import DashboardNavbar from './DashboardNavbar'

const Example = () => {
  return <DashboardNavbar handleDrawerToggle={handleMobileDrawerToggle} />
}
```

##### Used - `DashboardNavbar` is used in only vendor dashboard layout

- [VendorDashboardLayout](https://bazaar-doc.netlify.app/layouts/vendor-dashboard)

##### Props - `DashboardNavbar` Prop List:

<br />

<table>

<thead>
  <tr>
    <td>
      <b>Name</b>
    </td>
    <td>
      <b>Type</b>
    </td>
    <td>
      <b>Required</b>
    </td>
    <td>
      <b>Default</b>
    </td>
  </tr>
</thead>

  <tbody>
    <tr>
      <td>handleDrawerToggle</td>
      <td>function</td>
      <td>true</td>
      <td>-</td>
    </tr>

  </tbody>
</table>
