<h1 className="text-primary">Layouts</h1>

Folder location: `src/components/layouts`

<br/>

###### 1. `ShopLayout1`


<table>
<tbody>
    <tr>
        <td><img src="/ShopLayout1.jpg" width="300" /></td>
    </tr>
</tbody>
</table>

`ShopLayout1` is used in following home pages
```ts
2. '/pages/market-1.tsx'
2. '/pages/market-2.tsx'
3. '/pages/grocery3.tsx'
4. '/pages/gift-sho.tsx'
5. '/pages/gadget-shop.tsx'
6. '/pages/furniture-shop.tsx'
7. '/pages/fashion-shop.tsx'
8. '/pages/fashion-shop-2.tsx'
9. '/pages/fashion-shop-3.tsx'

```


<br/>
###### 2. `ShopLayout2`

<table>
<tbody>
    <tr>
        <td><img src="/ShopLayout2.jpg" width="300" /></td>
    </tr>
</tbody>
</table>

`ShopLayout2` is used in following home pages

```ts
1. '/pages/grocery1.tsx'
2. '/pages/grocery2.tsx'
3. '/pages/healthbeauty-shop.tsx'
```

<br/>

###### 3. `CustomerDashboardLayout`

`CustomerDashboardLayout` is used in following home pages

```ts
1. Wish List -> '/pages/wish-list'
2. Support Tickets -> '/pages/support-tickets'
3. Orders and Order Details Page -> '/pages/orders'
4. Profile and Edit Profile Page -> '/pages/profile'
5. Address and Address Details page -> '/pages/address'
6. Payment Methods and Payment Method Details Page -> '/pages/payment-methods'
```

<br/>


###### 4. `VendorDashboardLayout`

`VendorDashboardLayout` is used in following home pages

```ts
1. Inside vendor folder all pages -> '/pages/vendor'
2. Inside admin folder all pages -> '/pages/admin'
```

<br/>

###### 5. `ChcekoutNavLayout`

`ChcekoutNavLayout` is used in following home pages

```ts
1. Cart -> '/pages/cart'
2. Checkout -> '/pages/checkout'
3. Payment -> '/pages/payment'
```

<br/>

###### 6. `SaleLayout`
`SaleLayout` is used in following home pages

```ts
1. Sale Version 1 -> '/pages/sale-page-1'
2. Sale Version 2 -> '/pages/sale-page-2'
```